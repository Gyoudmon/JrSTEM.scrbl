%% employing the CTeX for better localization
%% `fandol` is also the default font set,
%%   it has trouble in displaying rarely used characters
\usepackage[heading = true, fontset = fandol]{ctex}

\setcounter{tocdepth}{1}
\setcounter{secnumdepth}{3}

%% reset chapter counters for parts
\usepackage{chngcntr}
\counterwithin{chapter}{part}

%% set chapter format
\ctexset{
  chapter/name = {},
  chapter/number = \arabic{chapter},
  section/number = \thesection,
  section/format = \Large\bfseries
}
